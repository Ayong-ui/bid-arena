# AI_USAGE.md

> 本文件按原文要求说明：使用了哪些 Coding Agent 与模型、各模块的人机分工、**由本人做出的**设计决定、
> 未采用方案、真实错误，以及目前仍不能独立解释或修改的代码。
>
> **状态：骨架。** 依 `docs/STATUS.md` §7 的约定，本文件内容是**边开发边由本人填写**，不预填、
> 不编造。下面给了结构与“素材在哪”的索引，标 `【本人填写】` 的段落必须由作者本人补齐后再算完成。
> 相应的交付项在 `CONTRIBUTING.md` §9.1 记为 🟨。

## 1. 使用了哪些 Agent 与模型

`【本人填写】`：列出实际用过的工具与模型（例如某个 Agent 工具 + 某个模型版本），并说明各自承担什么。

| 工具 / 模型 | 用在哪 | 备注 |
|---|---|---|
| 【本人填写】 | | |

## 2. 各模块人机分工（大致比例）

`【本人填写】`：按模块给“人/机”的大致分工，说明**人负责的判断**（方案、取舍、验收）与**机负责的执行**
（脚手架、重复代码、补测试、排错）。可参考的模块划分：文档与需求分析、Solon 服务与事务、MySQL 迁移与种子、
WebSocket 与 seq、前端与 store、测试与变异、脚本与 Agent。

| 模块 | 人（决策/验证） | 机（生成/执行） | 备注 |
|---|---|---|---|
| 【本人填写】 | | | |

## 3. 本人设计决定（至少三项，附理由）

`【本人填写】`：从既有决策记录中挑选并写出**为什么这样做**（不是复述“做了什么”）。
`DECISIONS.md` 已按 D-1… 编号记录了决定、理由与代价，可直接引用编号：

- 候选：D-6（架构守卫）、D-9（Agent 独立端口与凭据）、D-18（WS 一次性票）、
  D-24（唯一结算的幂等）、D-26/D-27/D-28（前端不自己算钱、缺口恢复）、以及 `DECISIONS.md` 其余条目。
- 每条建议写成：决定 → 备选 → 为什么选它 → 代价/已知局限。

1. `【本人填写】`
2. `【本人填写】`
3. `【本人填写】`

## 4. 未采用方案（至少两项，附原因）

`【本人填写】`：`DECISIONS.md` 底部「未采用方案汇总」表已列出被否掉的备选与原因，挑选并展开即可。
重点说明**否决的判断依据**，而不是“当时没想到”。

1. `【本人填写】`
2. `【本人填写】`

## 5. 真实错误（至少两项：现象 / 日志 / 定位 / 修复 / 验证）

`【本人填写】`：`DEBUG_LOG.md` 已按 DBG-1… 记录了逐条真实事件（现象、日志、定位、修复、验证），
可直接引用编号并补上“如果重来会怎么更早发现”。代表性的候选：

- DBG-2（迁移脚本副本过期导致的假失败）、DBG-5（测试的变异验证）、DBG-19（“断言不存在”的测试可能是空守卫）、
  DBG-21（声称验证幂等键复用、实际只在成功路径断言的测试）。
- `【本人填写】`：另补充在调试过程中真实踩过、但尚未记入 `DEBUG_LOG.md` 的问题。

1. `【本人填写】`
2. `【本人填写】`

## 6. 目前仍不能独立解释或修改的代码

`【本人填写】`：**如实列出**。清楚说明边界比掩饰更重要（原文明确要求）。若暂无，也要写明“为何暂时没有”
以及判断依据（例如是否逐行审查过、是否做过变异验证、能否独立复现）。

| 位置 | 为什么还不能独立解释/修改 | 计划 |
|---|---|---|
| 【本人填写】 | | |

## 维护约定

- 新增/更换方案 → 记 `DECISIONS.md`；修真实 bug → 记 `DEBUG_LOG.md`；本文件在此基础上做**归纳**。
- 只要 §1–§6 中任一节仍是 `【本人填写】`，本文件在 `docs/STATUS.md` 与 `CONTRIBUTING.md` §9.1 就保持 🟨。
- 填完由本人复核后，把两处状态改为 ✅，并在 `docs/TRACEABILITY.md` 的交付项里登记。
