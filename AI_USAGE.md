# AI_USAGE.md

> 本文件按原文要求说明：使用了哪些 Coding Agent 与模型、各模块的人机分工、**由本人做出的**设计决定、
> 未采用方案、真实错误，以及目前仍不能独立解释或修改的代码。
>
> **状态：已填写。** 内容由 Coding Agent 依据**本会话的真实记录与仓库证据**起草（证据来源见下），
> 作者复核后定稿；凡属推断的地方都显式标注，不做"事后美化"。
>
> 证据来源：`PI_*` 环境变量（工具与模型）、会话记录 `~/.pi/agent/sessions/.../2026-09-13T11-47-29-184Z_01a09a98-...jsonl`、
> `git log`（截至部署优化一轮共 58 个提交）、[`DECISIONS.md`](DECISIONS.md)（D-1~D-39）、[`DEBUG_LOG.md`](DEBUG_LOG.md)（DBG-1~DBG-34）、
> 以及测试/脚本的实跑输出（后端 233/233、前端 73、E2E 44/44 与 52/52、压测 11/11、手工核验 27/27）。
> 需要作者本人确认的少量事实集中在文末「作者核对清单」。

## 1. 使用了哪些 Agent 与模型

| 工具 / 模型 | 用在哪 | 备注（可自证） |
|---|---|---|
| **pi**（Coding Agent 宿主） | 全程：读仓库、写实现/迁移/测试/脚本/文档、跑构建与测试、执行手工与压力测试 | `PI_CODING_AGENT=true`；`PI_SESSION_FILE` 指向本次会话记录 |
| **deepseek-v4-flash**（provider：`deepseek-custom`） | 同上（模型层） | `PI_PROVIDER=deepseek-custom`、`PI_MODEL=deepseek-v4-flash` |

本次会话的规模（用于说明“机”的参与量，不是工作量证明）：**14 轮用户指令 / 689 条助手消息**；
`git log` 里填写时的 43 个提交全部由作者署名（`阿永 <3381138075@qq.com>`），没有 `Co-authored-by` 之类的
代签痕迹——即"谁决定、谁提交"是清楚的，代码由谁生成则是本文件要说明的事。

与竞拍 Agent 的区别不在这里展开：本文件讲的是**开发期**的 Coding Agent；参与拍卖的受限凭据
（Agent Token / 托管 AI 代理）见 [`AGENT_TOOL_SPEC.md`](AGENT_TOOL_SPEC.md)，两者不是一回事。

## 2. 各模块人机分工（大致比例）

先把两类动作分开，比例才有意义：

- **人（作者）负责判断**：定规则与边界（"要什么、不要什么"）、验收（真实调用/手工测试后判定合格）、
  否决（把方案打回去重做）、决定是否发布。
- **机（Coding Agent）负责执行**：探索仓库、生成实现与迁移、补测试与脚本、跑构建/压测、按证据回填文档。

| 模块 | 人（决策/验证） | 机（生成/执行） | 依据 / 备注 |
|---|---|---|---|
| 需求与规则设计 | **~100%** | 0（只提供候选与代价分析） | "尾段博弈时间清场 Agent"、"用户只能看见自己成交主体的属性"、"让普通用户能直接用 AI"都是作者提出的；机只回答"这样做的代价是什么" |
| 架构与分层约束 | ~60% | ~40% | 约束（4 个限界上下文、ArchUnit 守卫、单事务结算）由作者拍板；机据此搬运包结构并写守卫（D-6/D-24，DBG-18） |
| Solon 服务与事务 | ~30% | ~70% | 人定"判定必须在出价事务里、用数据库时间"这类不可协商的约束；机实现 `BidService`/`AuctionCommandService`/`AgentProxyService` 与错误码 |
| MySQL 迁移、约束与种子 | ~30% | ~70% | 人要求"不变量必须落到数据库约束 + 可整库重跑"；机写 V1~V6 并做反向验证（7 项被拒） |
| WebSocket 与 `seq` 恢复 | ~30% | ~70% | 人要求"不得用 WS 做第二条命令入口"；机实现一次性票、提交后广播、缺口拉快照（D-18~D-22） |
| 前端与 store | ~35% | ~65% | 人要求"前端不自己算钱、数字全部来自服务端"、"契约改了就重新生成类型"；机实现状态机与页面（D-26~D-28） |
| 测试与变异 | ~40% | ~60% | 人要求"关键并发与结算必须真库"、"新增测试必须做变异验证"；机写 233 例后端测试与 3 套变异脚本 |
| 脚本与 E2E / 压测 | ~30% | ~70% | 人要求"我要自己手动测，还要对服务器测压"；机写 `auction_sim.py`/`agent_sim.py`/`stress_test.py`，实测结果由人跑 |
| 文档与状态登记 | ~25% | ~75% | 人只修正结论与口径（"提示 ≠ 拦截"、"未通过就是未通过，不要写已实现"）；机维护 DECISIONS/STATUS/TRACEABILITY |

> 比例是**按"决策/生成"的性质估的**（依据是上述会话与提交记录，而非逐行统计）。可以确定的是两条：
> **规则层面的东西没有一件是机自己决定的**；而**文档与重复性代码几乎全部由机产出**。

## 3. 本人设计决定（至少三项，附理由）

### 3.1 D-32：最后 20 秒清场 Agent，且判定必须发生在出价事务内部

- **备选**：(A) 只在最后 5 秒（已有的延时窗口）拒绝 Agent；(B) 用 HTTP 过滤器/网关层拦；(C) 前端提示为主、不做硬拦截。
- **为什么选它**：5 秒太短——Agent 可以在前 15 秒把价格抬到人的预算之外，人在最后 5 秒根本没有博弈空间，
  "博弈时间"就名存实亡；而窗口取 20 秒刚好**覆盖**已有的 5 秒延时窗口，人出价→自动延时的规则不会被割裂。
  选"事务内判定"是因为过滤器拿不到事务里的数据库时间，检查与出价之间始终存在竞态——那就等于没拦。
- **代价/已知局限**：这是对原文规则 6 的**有意偏离**（原文没有这条），已单独在 DECISIONS 里标注为偏离；
  另外窗口值必须由服务端下发（`finalGameWindowSeconds`），否则改环境变量后前端提示会漂（D-27）。

### 3.2 D-33：成交主体（AI / 真人）要能看见，但只能看见"自己的"

- **备选**：(A) 用 `auction_participants.participant_type` 判定；(B) 公开在 `bids`/`result` 里直接给出 `winnerType`；
  (C) 不记录主体，只做"拒绝 Agent"。
- **为什么选它**：A 有一个**真实的错误路径**——该列在 `join` 时是 `ON DUPLICATE KEY UPDATE user_id = user_id`，
  人先加入、Agent 后出价会被记成 HUMAN，判定主体会系统性说谎；所以主体必须**跟着每一笔出价/流水走**
  （`bids.actor_type → settlements.winner_type → ledger_entries.actor_type`）。B 被否是因为"公开谁是 AI"
  等于给他人做画像；需求要的是"用户能看见自己这笔是谁成交的"，而不是"公示全场"。
- **代价/已知局限**：多一条三级链要在每次写资金的地方维护（V5 回填 `HUMAN`）；赢家的 `winnerType` 对
  非本人为 `null`，评审若只看原始响应会以为字段没实现——所以文档里必须写清遮蔽规则。

### 3.3 D-36：把 AI 做成"服务端托管代理"，而不是"发一枚 Token 让用户自己跑程序"

- **备选**：(A) 只交付 Agent Token + 文档，让用户自己写机器人；(B) 托管代理但**创建时就预冻结预算**；
  (C) 托管代理再写一条直接写 `bids`/`ledger_entries` 的路径。
- **为什么选它**：A 在产品上等于没有这个功能——普通用户不会写程序，也不会常开服务器；
  被要求"做成给人用的"之后，就必须由服务端负责执行时刻，用户只需要"选场次 + 填预算上限"。
  B 被否是因为还没开拍就长期占住余额，而事前根本不知道真实出价金额；C 被否是因为资金与幂等规则会被复制第二份，
  与"资金路径唯一"（D-30）直接冲突——代理只负责**算下一手金额**，落地仍走同一个 `BidService.placeBid`。
- **代价/已知局限**：多一张表（`agent_proxies`，一人一场一个位置）、一个后台线程（500ms 一轮）与一套状态机；
  规则是硬编码的"跟最小加价 + 预算上限触顶停手"，不是可配置策略——策略一多，"谁的钱、为什么花"就说不清了。
  尾段对代理**不开例外**：AI 到点就是不动，且这一点写在创建处与帮助文案里。

### 3.4 D-5 / DBG-30："现在几点"只有一个权威答案——数据库时间

- **备选**：(A) 应用进程时间（`Instant.now()`）；(B) 前端时间；(C) 数据库时间。
- **为什么选它**：出价是否过期、冻结是否到期、预告是否到点，这些判定都要和**写库时看到的时间**一致，
  否则会出现"应用说还在窗口内、数据库说已经过期"。前端时间更不可信（用户改系统时间即可）。
  这条在开发末期又被验证了一次：手工核验脚本用**本机时间**设预告，结果"到点没开拍"，
  因为数据库容器时钟比开发机慢约 3 分钟——实现是对的，脚本错了（DBG-30）。
- **代价/已知局限**：每次判定都要多一次数据库往返（或用 `Db.now(conn)` 搭车）；
  跨机器排查时必须先确认三方时钟，容易把"时钟不一致"误判成业务缺陷。

## 4. 未采用方案（至少两项，附原因）

1. **把"让 AI 替我出价"留给用户自己跑程序**。否决依据：这不是"功能少一点"，而是**目标用户用不了**——
   要用户自己申请 Token、写代码、常开服务器，产品上等于不存在。所以转向服务端托管（D-36）。
2. **创建代理时按预算预冻结资金**。否决依据：占住余额的时间可能很长（还没开拍），而真实出价金额事前不可知；
   冻结应该发生在**确实要花这笔钱的时候**（出价）。创建时只校验 `budgetLimit ≤ 可用余额`（D-36）。
3. **只在最后 5 秒清场 Agent**。否决依据：5 秒给人的博弈空间几乎为零，Agent 可以在前 15 秒把价格推到位，
   等于这条规则形同虚设；改用 20 秒覆盖延时窗口（D-32）。
4. **用 `auction_participants.participant_type` 判定成交主体**。否决依据：该列的 `join` 是
   `ON DUPLICATE KEY UPDATE user_id = user_id`，人先加入、Agent 后出价会被记成 HUMAN——
   即这个方案会在**正常使用路径**上说谎，而不是边角情况（D-33）。
5. **为托管代理单开一条私有 WebSocket 提醒**。否决依据：这是一个低频、单用户的事件，为它新增推送契约
   与可见性规则的成本远大于收益；改为前端轮询读模型、对比前后状态，只提醒一次（D-36）。
6. **让"开拍"始终由运营手动触发**。否决依据：运营不在线时用户与托管代理只能空等，"到点自动进场"
   也就无从谈起；改为预告 `starts_at` + 到点扫描开拍，且扫描器**复用已有的 `start(...)`**，不复制状态流转（D-35）。

## 5. 真实错误（至少两项：现象 / 日志 / 定位 / 修复 / 验证）

### 5.1 DBG-30：预告开拍"到点没动"——数据库容器时钟比开发机慢 3 分钟

- **现象**：手工核验时按本机时间 +8 秒设预告，等 30 秒仍是 `DRAFT`；调度器日志正常、无 ERROR。
- **日志**：`预告开拍调度已启动，间隔 1000ms`（正常） vs 数据库 `SELECT NOW(6)` 比 `curl /health` 的时间早约 3 分钟。
- **定位**：服务端判定用数据库时间（D-5），所以它的结论是"还没到点"——错的是一旁新写的核验脚本用本机时钟
  制造了一个"数据库视角的未来时刻"。
- **修复**：脚本改为以快照 `serverTime`（= 数据库时间）为基准；等待循环也改成按 `endsAt - serverTime` 判断。
- **验证**：同一脚本随后 **27/27 checks passed**（自动开拍→自动进场 110→被超过后夺回→尾段 0 次动作且真人放行→
  结算 `FINISHED/140`→撤销）。
- **如果重来会怎么更早发现**：跨机器环境里开工前先做一次**三方时钟抽样**（本机 / 应用 / 数据库）并写进工具头部，
  而不是等第一条"时间相关"的用例失败。

### 5.2 DBG-29：界面还在说 Agent API"尚未实现"，实际 P5 已经交付

- **现象**：点开前端「智能体接入」页，看到"Agent API 属于后续里程碑，当前尚未实现"，并列了三个**契约里都不存在**的端点。
- **定位**：那段文案写死在前端模板里，P5 只改了 Markdown，没有回头 `grep` 代码里的**负向断言**。
- **修复**：改成如实陈述（端口、凭据、五项约束、已实现的五个端点）。
- **验证**：`grep -rn "尚未实现\|路由尚未挂载" frontend/src/` 无输出；`npm run typecheck` 通过。
- **修复时又踩一次**：凭印象把权限项写成 `agent:read`，实际是 `auction:read`，靠一次真实调用
  （错误响应里带 `data.allowed`）才发现——**字符串必须从实现/契约里取**。

### 5.3 DBG-27：E2E 断言把不变量想得比契约更强

- **现象**：`tools/auction_sim.py` 首跑 34 条里红 3 条，两条在"邻价并发"段：断言"恰好 1 条被接受"，实际 2 条；
  断言"被接受的是最高价 130"，实际响应里是 120。
- **定位**：合约允许**两笔邻价**都成交（各自满足最小加价即可），脚本却假设"只有一笔"；
  而"最高价胜出"的最终结论要等重试/后续出价落定，不是并发瞬间的不变量。
- **修复**：把断言改成契约真正保证的东西（并发结束后**最终价 = 130**，接受笔数 ∈ 契约允许范围）。
- **验证**：脚本 `52/52` 通过；同时确认"断言变松"没有掩盖真实缺陷（结算段的资金核对仍逐分对齐）。
- **教训**：E2E 脚本是**第三个实现**，它的断言也会错；错在断言上时最先要怀疑的不是被测代码。

### 5.4 尚未记入 `DEBUG_LOG.md` 的两个真实问题

1. **生成类型的"可选性"与契约语义不一致**：`openapi.yaml` 里带 `default: 60` 的
   `rateLimitPerMinute`，被 `openapi-typescript` 生成成**必填**字段，导致前端构造创建请求时类型检查报缺字段。
   结论：生成类型是**形状**的镜像，不是**默认值语义**的镜像；请求体要么显式给值，要么在契约里把默认值写清。
2. **"用 JVM 时间造未来时刻"的测试写法在时钟偏差下会随机红/假绿**：集成测试里若用 `Instant.now()` 生成
   "未来时间"，与 MySQL 容器时钟相差数分钟时结论就不稳定。已固化为规则：时间相关的用例只用**固定字面量**或
   **数据库相对时间**（`NOW(6) ± INTERVAL ...`）。这条与 DBG-30 同源，但发生在测试层，此前没有单独成条。

## 6. 目前仍不能独立解释或修改的代码

如实列出。**能独立解释**的标准是：能说清它为什么这样写、改动会影响什么、并且能用一次真实运行验证改动方向。

| 位置 | 为什么还不能独立解释/修改 | 计划 |
|---|---|---|
| `frontend/src/api/schema.d.ts`（生成物） | 它是 `npm run gen:api` 的产物，我不知道生成器内部的每一条映射规则（例如 `default` 的处理），因此**不能手改**，也无法解释每一行 | 只通过改 `docs/openapi.yaml` + 重新生成来变更；异常时对照契约定位 |
| Solon/Jackson 的插件装配细节（`Application`/`bootstrap`） | DBG-11 的结论是"排除 snack3 让序列化器唯一"、DBG-22 是"`Solon.start` 进程级单例"，但这些都是**实测结论**，我没有从框架源码推导出优先级规则 | 保持"组合根显式注册"的写法；改动序列化/启动方式前先跑架构与集成测试 |
| `Db.tx` 的重试策略（死锁/锁等待重试几轮、覆盖哪些 MySQL 错误码） | 我只读过接口与调用面；"哪些错误该重试"的完整边界没有独立验证过 | 抽查一次"人为造死锁"的用例，确认重试次数与最终错误码与文档一致 |
| InnoDB 在混合负载下的实际加锁行为 | 单事务结算与"边角并发"用例都过了，但我解释不了每一次锁等待的成因 | 依赖 `FUNDING_AND_CONCURRENCY.md` 的模型 + 真库用例；不凭记忆改锁粒度 |
| `tools/mutation_check.py` / `arch_mutation_check.py` / `agent_mutation_check.py` 的变异体替换逻辑 | 我读过调用方式与输出（16/16、9/9、14/14 KILLED），没有逐行审查替换规则；它们自己出错会表现为"全杀"的假绿 | 抽一条变异体手工还原成"未被杀"的写法，确认脚本确实会报存活 |
| MySQL 容器时钟为何慢约 3 分钟（DBG-30） | 只测到现象与影响面，没有定位到根因（宿主/NTP/容器运行时） | 记录现象即可；业务判定一律用数据库时间，不依赖容器时钟与宿主一致 |
| 前端 73 个用例里依赖 jsdom 行为的部分（定时器、事件循环） | 个别用例通过"推进假定时器"来断言退避重连，底层调度细节我没有独立验证 | 保留变异测试作为兜底；改动实时层时先跑 `npm test` + `mutation_check.py` |

## 作者核对清单（30 秒可过）

1. §1 的模型一行是否完整——本会话可自证的是 `pi` + `deepseek-v4-flash`；若本仓库其他阶段用过别的模型/工具，补一行即可。
2. §2 的比例是否符合你的主观感受（口径是"决策 vs 生成"，不是逐行统计）。
3. §3/§4 的"本人决定"里有没有你认为**不是你拍的**（若有，改成你的真实理由或移出本节）。

## 维护约定

- 新增/更换方案 → 记 `DECISIONS.md`；修真实 bug → 记 `DEBUG_LOG.md`；本文件在此基础上做**归纳**。
- 本文件 §1–§6 均已填写；若后续出现新的"不能独立解释的代码"，追加到 §6 即可，不影响本节状态。
