# 跑腿项目复用盘点

## 当前结论

截至 2026-09-11，在 `D:\Study\项目实战\Projects` 下未定位到明确的“跑腿/接单/配送”交易项目源码。发现的 `his-sample-agent-travel` 是旅行规划 Agent，不具备跑腿订单语义，因此本项目暂不直接复制其业务代码。

## 复用策略

| 分类 | 处理方式 |
|---|---|
| 可直接复用 | 待获得真实跑腿源码后，优先复用用户认证、角色、公共响应、异常处理、Docker 配置 |
| 适配后复用 | 钱包/流水、冻结解冻、订单状态机、WebSocket、前端布局和 Store；必须先验证锁与事务边界 |
| 不复用 | 跑腿订单、配送、接单、评价、位置等业务语义 |

## 当前独立骨架

拍卖核心暂以纯 Java 领域引擎实现，便于先验证状态机、并发入口、幂等、冻结/释放、延时和唯一结算。后续接入真实项目时，只替换持久化与认证适配层，不改变规则层。
