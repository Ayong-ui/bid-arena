# Bid Arena：业务理解与产品原型分析（供 AI 使用）

## 1. 文档定位

本文把《Bid Arena：多人实时拍卖间》评测题转换为可供 AI 设计、编码、测试和评审的产品规格。它不是实现代码，也不替代原题中的 `DESIGN.md`、`DECISIONS.md`、`DEBUG_LOG.md` 等交付物。

核心原则：**MySQL 是唯一事实来源，服务端是唯一业务裁判，前端和竞拍 Agent 只能发起意图并展示结果。** 所有金额均为测试虚拟积分，不涉及真实支付。

## 2. 产品目标与范围

### 2.1 目标

- 支持管理员创建、开始、取消限时拍卖。
- 支持真人、模拟用户和受限竞拍 Agent 同场加入与出价。
- 在并发出价、重复请求、延时、断线、重启和重复结算下保持资金与赢家一致。
- 用浏览器界面完成核心操作，并用脚本和自动化测试证明业务正确性。

### 2.2 明确不做

真实支付、复杂运营后台、聊天、推荐系统、精细美术、客户端决定赢家、Agent 直接访问数据库或管理接口均不在范围内。

### 2.3 成功标准

评审可在约 5 分钟内通过 Compose 启动系统、使用种子账号开始演示拍卖、打开两个浏览器参与、运行 20 人模拟、让 Agent 出价、等待结算并核对钱包/流水/成交结果。

## 3. 用户与权限

| 角色 | 目标 | 权限边界 |
|---|---|---|
| 管理员 | 准备并控制拍卖 | 创建、开始、取消拍卖；签发/吊销 Agent Token；查看必要的系统结果 |
| 真人竞拍者 | 在网页加入并竞价 | 查看公开拍卖和参与者允许的数据；加入；提交出价；查看自己的钱包和结果 |
| 模拟用户 | 验证并发与规则 | 使用公开 API 执行与真人同等的加入、出价和快照恢复 |
| 竞拍 Agent | 自动决策出价 | 仅用 Token 访问指定 auction；按权限读取和出价；受过期、吊销和频率限制约束 |
| 服务端 | 业务裁判 | 校验身份、状态、价格、余额、锁、幂等、延时、结算和通知 |

## 4. 核心领域模型

### 4.1 拍卖状态机

```text
DRAFT --start--> RUNNING --到期--> SETTLING --结算成功--> FINISHED
  |                  |
  +----cancel--------+----cancel-------------------------> CANCELLED
```

- `DRAFT` 可开始或取消；`RUNNING` 可出价或取消。
- `SETTLING` 后不可取消、不可出价。
- 仅 `RUNNING` 接受出价，截止判断使用服务端接收时间且必须严格早于 `ends_at`。
- 定时扫描和服务启动恢复都要驱动 `RUNNING → SETTLING → FINISHED`，不依赖客户端。

### 4.2 默认业务常量

| 项目 | 默认值 |
|---|---:|
| 起拍价 | 100 |
| 最小加价 | 10 |
| 默认时长 | 180 秒 |
| 测试用户初始总余额 | 1000 |
| 狙击窗口 | 截止前 5 秒内 |
| 每次延长 | 10 秒 |
| 最大延长次数 | 3 |

### 4.3 出价与资金不变量

- `available = total_balance - frozen_amount`，且两者始终非负。
- 新出价必须 `amount >= current_price + min_increment`。
- 用户在本场需要新增冻结 `max(0, amount - user_bid_frozen_in_auction)`；不能超过可用余额。
- 成为新领先者时：新领先者冻结差额，旧领先者释放本场冻结；同一用户再次领先只冻结差额。
- 失败出价不改变价格、领先者、冻结、余额和流水。
- 结算时赢家冻结转扣款，其他参与者冻结全部释放；无出价则无赢家、无扣款。
- 每场最多一条成交记录；资金流水必须能解释每一次冻结、释放和扣款。

## 5. 关键用户流程

### 5.1 管理员流程

1. 登录。
2. 查看草稿拍卖或创建拍卖（标题、描述、起拍价、最小加价、时长）。
3. 点击“开始”，服务端写入 `RUNNING` 和截止时间。
4. 需要时在 `DRAFT/RUNNING` 点击“取消”；服务端释放冻结并广播结果。
5. 创建 Agent Token：选择 Agent 用户、允许的拍卖、`auction:read`/`auction:bid`、过期时间、频率限制；明文仅显示一次。
6. 查看成交结果、钱包和资金流水。

### 5.2 真人竞拍者流程

1. 登录后查看拍卖列表。
2. 打开拍卖详情并点击加入。
3. 看到服务端快照：当前价、领先者匿名标识、剩余时间、参与人数、延时次数、连接状态。
4. 输入金额并提交；按钮应显示提交中、成功或拒绝原因。
5. 接收实时事件；发现 `seq` 缺口或重连后重新拉取快照。
6. 结束后查看自己是否获胜、冻结是否释放/扣款及流水。

### 5.3 竞拍 Agent 流程

1. 从安全的环境变量读取 Token，不写入代码、前端包、日志或命令历史。
2. 调用允许的拍卖快照接口。
3. 基于策略判断是否出价，生成唯一 `requestId`。
4. 提交出价并处理成功、幂等重试、权限、过期、频率限制和业务拒绝。
5. 通过快照或结果接口核对最终状态。

## 6. 产品原型（页面与组件）

### 6.1 页面地图

```text
/login
  └─ 登录表单
/auctions
  ├─ 拍卖卡片列表（状态、当前价、倒计时、参与人数）
  └─ /auctions/:id
       ├─ 实时状态区
       ├─ 出价区
       ├─ 最近出价表
       ├─ 参与者/连接状态
       └─ 结果与资金摘要
/admin/auctions
  ├─ 创建拍卖
  ├─ 开始/取消
  └─ 创建 Agent Token（一次性明文展示）
/wallet
  └─ 总余额、冻结余额、可用余额、流水
```

### 6.2 拍卖列表页

- 顶部：当前用户、钱包可用余额、退出。
- 主区：按 `RUNNING/DRAFT/FINISHED/CANCELLED` 分组的拍卖卡片。
- 卡片字段：标题、状态徽标、当前价、起拍价、截止倒计时、延长次数、参与人数、进入详情按钮。
- 倒计时仅用于展示；服务端时间和接口状态决定可否出价。

### 6.3 拍卖详情页

- **状态头部**：标题、状态、服务端校准后的倒计时、当前价、领先者、延长次数/上限。
- **出价面板**：金额输入、最低允许金额提示、提交按钮、最近一次请求状态、拒绝原因。
- **事件/出价表**：服务端序号、时间、金额、用户匿名标识、结果；不泄露他人私有钱包。
- **连接提示**：已连接、重连中、已通过快照同步；连接断开时禁用“假实时”视觉。
- **结束面板**：赢家、成交价、结算原因、本人是否获胜、扣款/释放摘要。

### 6.4 管理员页面

- 创建表单：名称、描述、起拍价、最小加价、持续秒数。
- 操作按钮按状态可用性显示：草稿可开始/取消，运行中可取消，结算中全部禁用。
- Token 表单显示权限、拍卖范围、过期时间、频率限制；创建成功后高亮一次性 Token，并提供复制按钮与“已保存则不可再次查看”提示。

### 6.5 钱包页

展示 `totalBalance`、`frozenAmount`、`availableBalance`，以及按时间倒序的资金流水：类型（冻结/释放/扣款）、金额、关联拍卖、关联 requestId、时间、方向。余额数字来自服务端快照，不由前端累加推导。

## 7. API 与实时协议建议

### 7.1 HTTP

```text
POST /api/v1/auth/login
GET  /api/v1/users/me
GET  /api/v1/wallets/me
POST /api/v1/admin/auctions
POST /api/v1/admin/auctions/{id}/start
POST /api/v1/admin/auctions/{id}/cancel
POST /api/v1/admin/agent-tokens
GET  /api/v1/auctions
GET  /api/v1/auctions/{id}
POST /api/v1/auctions/{id}/join
POST /api/v1/auctions/{id}/bids
GET  /api/v1/auctions/{id}/bids
```

出价请求：`{ requestId: string, amount: number }`。响应应可区分新处理、幂等重放、业务拒绝和鉴权错误，并返回服务端状态或事件序号。

### 7.2 WebSocket 事件

每个确认事件都包含：`auctionId`、单场单调递增 `seq`、`serverTime`、`type`。建议事件：

| 事件 | 广播范围 | 用途 |
|---|---|---|
| `AUCTION_SNAPSHOT` | 参与者 | 初次连接/重连后的权威状态 |
| `PARTICIPANT_JOINED` | 参与者 | 更新人数与参与者列表 |
| `BID_ACCEPTED` | 参与者 | 更新当前价、领先者、资金相关公共信息 |
| `BID_REJECTED` | 仅请求者 | 展示低价、余额、状态、迟到等原因 |
| `AUCTION_EXTENDED` | 参与者 | 更新截止时间和延长次数 |
| `AUCTION_FINISHED` | 参与者 | 展示唯一成交结果 |
| `CONNECTION_STATE` | 本人 | 连接和快照同步状态 |

广播失败不回滚数据库事务。客户端收到缺口（例如上次 `seq=12`，本次为 `14`）必须重新请求详情/快照；不能用本地猜测补齐 `13`。

## 8. 服务端事务建议（供 AI 实现时遵循）

### 8.1 原子出价

在一个 MySQL 事务内：

1. 以行锁读取拍卖、当前参与者冻结和幂等记录。
2. 先判断 `RUNNING`、服务端当前时间 `< ends_at`、金额下限、加入关系和 requestId。
3. 锁定新用户钱包，计算本场已冻结金额与需新增冻结，执行余额条件更新。
4. 若超过当前价，锁定旧领先者钱包并释放旧冻结；写入新冻结/流水。
5. 更新拍卖当前价、领先者、截止时间、延长次数；写入出价和单场递增 `seq`。
6. 提交事务后再发布 WebSocket 事件；事件携带提交后的快照字段。

实现可选择悲观行锁、版本号乐观锁或串行化队列，但必须说明锁顺序、死锁重试、唯一约束和失败边界。推荐稳定的锁顺序（拍卖 → 钱包按 userId 排序）避免并发死锁。

### 8.2 幂等

用 `(auction_id, user_id, request_id)` 唯一约束保存原始请求、结果状态和关联出价/错误。重复请求返回第一次结果，不再次改余额、价格、seq 或流水。requestId 的用户隔离和长度/格式校验必须明确。

### 8.3 唯一结算

定时任务扫描已到期 `RUNNING` 拍卖；以条件更新或行锁抢占，将一场拍卖置为 `SETTLING`。随后在事务内锁定拍卖和参与者钱包：赢家冻结转扣款，其他冻结释放，写入唯一成交记录和流水，改为 `FINISHED`。失败可重试，必须做到重复触发不重复扣款。服务启动时先扫描遗留的到期 `RUNNING/SETTLING` 记录。

### 8.4 取消

仅 `DRAFT/RUNNING` 可取消。`RUNNING` 取消应在一致性边界内释放所有参与者冻结、写流水、改 `CANCELLED` 并广播；`SETTLING` 之后拒绝取消。

## 9. 数据库建议

最低表集合：

| 表 | 关键字段/约束 |
|---|---|
| `users` | id、email、password_hash、role、status |
| `wallets` | user_id 唯一、total_balance、frozen_amount、version |
| `ledger_entries` | user_id、type、amount、auction_id、request_id、created_at；业务关联索引 |
| `auctions` | status、start_price、min_increment、current_price、leader_id、ends_at、extension_count、seq |
| `auction_participants` | auction_id + user_id 唯一、joined_at、participant_type |
| `bids` | auction_id、user_id、amount、request_id、server_seq、server_time；request 唯一约束 |
| `settlements` | auction_id 唯一、winner_id、final_price、reason、created_at |
| `agent_tokens` | token_hash、agent_user_id、auction_id/范围、permissions、expires_at、revoked_at、rate_limit |
| `bid_requests` | auction_id、user_id、request_id 唯一、状态、响应快照/错误码 |

金额使用整数积分或精确 decimal，禁止浮点。时间统一由服务端生成并明确时区/精度。Token 只存摘要，日志脱敏。

## 10. 测试与演示验收清单

### 10.1 自动化测试

- 非法状态、低价、余额不足、截止后出价均拒绝且资金不变。
- 相同 requestId 重试只有一次出价、一次资金变化、一次 seq。
- 两用户并发出价结果唯一且可解释。
- 新领先者冻结、旧领先者释放、同用户只冻结差额。
- 最后 5 秒延长 10 秒，最多 3 次，第 4 次不再延长但仍可成功。
- 到期、取消、重复结算和重启恢复只产生一条成交结果。
- 结算后总余额、冻结余额、可用余额、流水和成交一致。
- Agent Token 的范围、权限、过期、吊销、频率限制有效。
- WebSocket seq 单调；断线重连可用快照恢复。
- 至少一个 Vue Store 或核心交互组件测试；并发/结算使用真实 MySQL 或容器集成环境。

### 10.2 多人模拟

默认命令示例：`npm run simulate:auction -- --auctionId=<id> --bots=20 --seed=42`。覆盖加入、普通出价、同/相邻价格并发、requestId 重试、余额不足/低价/已结束、最后五秒狙击、断线快照恢复和最终核对。输出每项断言与最终摘要。

### 10.3 五分钟演示路径

```text
复制 .env.example
→ docker compose up
→ 自动迁移/种子完成
→ admin 登录并开始演示拍卖
→ 两个浏览器用户加入并出价
→ 运行 20 人模拟脚本
→ 创建 Agent Token，让 Agent 查询并出价
→ 展示最后五秒延时
→ 等待/触发自动结算
→ 查看赢家、成交价、钱包和流水
→ 运行自动化测试
```

## 11. 给 AI 的实现提示词（可直接复用）

你正在实现 Bid Arena 多人实时拍卖间。请先阅读本文件和原文转录，输出数据库迁移、Solon 领域服务、Vue 页面、WebSocket 协议、OpenAPI、集成测试和模拟脚本的变更计划，再逐模块实现。严格遵守：MySQL 是唯一事实来源；服务端决定状态、价格、余额、截止时间和赢家；金额不能使用浮点；出价必须原子地完成幂等、锁、冻结/释放、价格、延时、流水和事件序号；广播失败不能回滚事务；重复结算不能重复扣款；服务重启后必须继续结算。每次修改后运行相关测试，展示 diff、失败原因和修复验证。不要生成真实 Token，不要把业务判断放到前端或 Agent。

## 12. 高风险点与评审追问

- 如何证明“价格已更新但冻结未成功”不可能提交？
- 两个用户并发且金额相邻时，锁的顺序是什么？死锁如何重试？
- requestId 重试发生在事务提交前、提交后、广播失败后分别返回什么？
- 定时任务和服务重启同时结算时，谁获得所有权？
- WebSocket 丢失 seq 后，快照与后续事件如何避免倒退？
- 取消拍卖批量释放冻结时，如何避免漏参与者或重复流水？
- Token 泄露、过期、吊销和跨拍卖访问如何验证？
- MySQL、Redis（若使用）和 WebSocket 状态不一致时，恢复源是什么？

## 13. 交付文档与工程证据

根目录至少应有 `.env.example`、Compose 文件、迁移/种子、README、OpenAPI、`AI_USAGE.md`、`DESIGN.md`、`DECISIONS.md`、`DEBUG_LOG.md`、`AGENT_TOOL_SPEC.md`，并保留能反映行为变化的 Git 提交历史。README 需说明未完成边界；调试日志必须记录真实现象、日志、定位、修复和验证，不得事后编造。
